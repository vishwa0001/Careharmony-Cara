# Preferred unified Cara campaign API URL, e.g. https://abc.lambda-url.us-east-1.on.aws
VITE_CAMPAIGN_API_BASE_URL=

# Optional bearer token supplied by an authenticated edge/API gateway.
# Do not embed long-lived AWS credentials or production secrets in Vite env files.
# VITE_CAMPAIGN_API_BEARER_TOKEN=
